---
name: placeholder-skill
description: "Placeholder skill directory required by skill-eval's run_eval tool signature. This benchmark runs configurations=['without_skill'] only — we are measuring raw model tiers, not a skill — so this file's content is never mounted or read by a subject session."
user-invocable: false
---

Not used. See benchmarks/model-tiers/SCHEMA.md and run_suite.py's module docstring
for why this directory exists.
