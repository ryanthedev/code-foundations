# Unused: Sensor now lives inline in tire_pressure_monitoring.py (self-contained
# starter). Kept only so an accidental `from sensor import Sensor` still resolves.
from tire_pressure_monitoring import Sensor  # noqa: F401
