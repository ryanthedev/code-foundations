"""Arm variants and the deterministic swap mechanism for the concise-doctrine A/B.

The `baseline` arm is a verbatim copy of the repo's current build-agent.md; the
`concise` arm adds the candidate concise-code doctrine. `swap` points a runner's
sandbox agent-file at the chosen variant with a guaranteed baseline-restore.
"""
